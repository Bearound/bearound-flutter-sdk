group = "com.example.bearound_flutter_sdk"
version = "1.0-SNAPSHOT"

buildscript {
    ext.kotlin_version = "2.1.0"
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath("com.android.tools.build:gradle:8.11.1")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version")
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        maven {
            url = uri("http://192.168.10.61:8081/repository/maven-releases/")
            setAllowInsecureProtocol(true)
        }
//        maven {
//            name = "GitHubPackages"
//            url = uri("https://maven.pkg.github.com/Bearound/bearound-android-sdk")
//            credentials {
//                username = System.getenv("GPR_USER")
//                password = System.getenv("GPR_KEY")
//            }
//        }
    }
}

apply plugin: "com.android.library"
apply plugin: "kotlin-android"

android {
    namespace = "com.example.bearound_flutter_sdk"

    compileSdk = 36

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_11
        targetCompatibility = JavaVersion.VERSION_11
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_11
    }

    sourceSets {
        main.java.srcDirs += "src/main/kotlin"
        test.java.srcDirs += "src/test/kotlin"
    }

    defaultConfig {
        minSdk = 21
    }

    dependencies {
        testImplementation("org.jetbrains.kotlin:kotlin-test")
        testImplementation("org.mockito:mockito-core:5.18.0")
    }

    testOptions {
        unitTests.all {
            useJUnitPlatform()

            testLogging {
               events "passed", "skipped", "failed", "standardOut", "standardError"
               outputs.upToDateWhen {false}
               showStandardStreams = true
            }
        }
    }
}

dependencies {
    //implementation("org.bearound:android-beacon-sdk:0.7-alpha")
    //implementation(name: 'sdk-release', ext: 'aar')
    //implementation(name: 'sdk-release', ext: 'aar', group: 'org.bearound', version: '0.1.1-alpha')
   //implementation fileTree(dir: '/Users/felipearaujo/Empresa/OpenCircle/bearound_flutter_sdk/android/libs', include: ['*.aar', '*.jar'], exclude: [])
    //implementation 'org.altbeacon:android-beacon-library:2.21.1'
//    implementation fileTree(dir: 'libs', include: ['*.aar', '*.jar'])
//    api 'org.altbeacon:android-beacon-library:2.21.1'
//    implementation 'com.google.android.gms:play-services-ads-identifier:18.2.0'
//    implementation "org.jetbrains.kotlinx:kotlinx-coroutines-android:1.10.2"
//    implementation 'androidx.lifecycle:lifecycle-process:2.9.2'
    //implementation 'org.bearound:android-beacon-sdk:0.7-alpha'
    implementation 'org.bearound:android-beacon-sdk:0.1.4-alpha'
}
